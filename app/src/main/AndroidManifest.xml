<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nghia02253.myandroid">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DatabaseActivity" android:label="SQlite Database"></activity>
        <activity android:name=".MediaAudioActivity" />
        <activity
            android:name=".VolleyObjectActivity"
            android:label="Volley Object" />
        <activity
            android:name=".VolleyStringActivity"
            android:label="Volley String" />
        <activity
            android:name=".LoadJSONArrayObjectActivity"
            android:label="Load JSON Array Object" />
        <activity android:name=".LoadJSONLanguageActivity" />
        <activity
            android:name=".LoadJSONActivity"
            android:label="Load JSON" />
        <activity
            android:name=".LoadImageInternet"
            android:label="@string/title_activity_load_image_internet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListUser" />
        <activity
            android:name=".ListUserActivity"
            android:label="Danh sách công việc"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResetPass" />
        <activity android:name=".LoadContentActivity" />
        <activity
            android:name=".LoadRssActivity"
            android:label="Danh sách tin RSS" />
        <activity
            android:name=".LoadRssDetailActivity"
            android:label="Chi tiết tin" />
    </application>

</manifest>